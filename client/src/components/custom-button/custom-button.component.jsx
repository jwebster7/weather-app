import React from "react";

import { CustomButtonContainer } from "./custom-button.styles";

const CustomButton = () => {
  return <CustomButtonContainer>Custom Button</CustomButtonContainer>;
};

export default CustomButton;
